# [Project Name] — Rules

> [One line: what this project is, what it sells, produces or manages.]

This file contains rules only. The state lives in `memory.md` in this folder.

## Scope

[What belongs to this project, what stays out and where it lives instead. Test: "is this still useful if I completely change [service/channel/relevant variable]?" Yes → here. No → elsewhere.]

## Structure

[Only if it is not obvious from the subfolder names: 2-3 lines on what each contains. If it is obvious, delete this section.]

## Rules inherited from root

- Macro propagation: this conversation [may / may not] write directly to the root `memory.md` and `archive.md` for macro events — [condition, if any].
- Memory → Archive: when a point in this folder's `memory.md` is replaced (not just added to), the old content goes first into this same folder's `archive.md`.
- [Other rules specific to this project, if needed.]

---
*To use: copy into `10_projects/<name>/CLAUDE.md`, fill in the placeholders, delete the sections that do not apply.*
